---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: toolbindings.agentspec.io
spec:
  group: agentspec.io
  names:
    kind: ToolBinding
    listKind: ToolBindingList
    plural: toolbindings
    singular: toolbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.name
      name: NAME
      type: string
    - jsonPath: .spec.toolType
      name: TYPE
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.boundAgentCount
      name: BOUND-AGENTS
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ToolBinding is the Schema for the toolbindings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToolBindingSpec defines the desired state of ToolBinding.
            properties:
              accessPolicy:
                description: AccessPolicy defines namespace-level access controls
                  for this tool.
                properties:
                  allowedNamespaces:
                    description: AllowedNamespaces lists the namespaces permitted
                      to reference this ToolBinding.
                    items:
                      type: string
                    type: array
                type: object
              command:
                description: Command holds the configuration when ToolType is "command".
                properties:
                  args:
                    description: Args is the list of arguments to pass to the binary.
                    items:
                      type: string
                    type: array
                  binary:
                    description: Binary is the path or name of the binary to execute.
                    type: string
                required:
                - binary
                type: object
              description:
                description: Description provides a brief explanation of what the
                  tool does.
                type: string
              http:
                description: HTTP holds the configuration when ToolType is "http".
                properties:
                  method:
                    description: Method is the HTTP method to use (GET, POST, PUT,
                      DELETE, etc.).
                    type: string
                  url:
                    description: URL is the endpoint URL for the HTTP tool.
                    type: string
                required:
                - method
                - url
                type: object
              mcp:
                description: MCP holds the configuration when ToolType is "mcp".
                properties:
                  serverRef:
                    description: ServerRef is the name of the MCP server resource
                      to connect to.
                    type: string
                required:
                - serverRef
                type: object
              name:
                description: Name is the human-readable name of the tool.
                type: string
              toolType:
                description: ToolType is the type of tool backing this binding.
                enum:
                - command
                - mcp
                - http
                type: string
            required:
            - name
            - toolType
            type: object
          status:
            description: ToolBindingStatus defines the observed state of ToolBinding.
            properties:
              boundAgentCount:
                description: BoundAgentCount is the number of agents currently using
                  this tool binding.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the ToolBinding's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastProbeTime:
                description: LastProbeTime is the last time the tool endpoint was
                  probed for health.
                format: date-time
                type: string
              phase:
                description: Phase is the current phase of the tool binding (Pending,
                  Ready, Error).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
