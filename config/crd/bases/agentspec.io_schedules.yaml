---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: schedules.agentspec.io
spec:
  group: agentspec.io
  names:
    kind: Schedule
    listKind: ScheduleList
    plural: schedules
    singular: schedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.name
      name: NAME
      type: string
    - jsonPath: .spec.schedule
      name: SCHEDULE
      type: string
    - jsonPath: .status.nextScheduleTime
      name: NEXT-RUN
      type: date
    - jsonPath: .spec.suspend
      name: SUSPEND
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Schedule is the Schema for the schedules API. It is namespace-scoped.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleSpec defines the desired state of a Schedule.
            properties:
              concurrencyPolicy:
                default: Forbid
                description: ConcurrencyPolicy specifies how to treat concurrent executions.
                enum:
                - Allow
                - Forbid
                - Replace
                type: string
              schedule:
                description: Schedule is a cron expression defining when to run the
                  task.
                type: string
              startingDeadlineSeconds:
                description: |-
                  StartingDeadlineSeconds is the deadline in seconds for starting the task
                  if it misses its scheduled time.
                format: int64
                type: integer
              successfulTasksHistoryLimit:
                default: 3
                description: SuccessfulTasksHistoryLimit is the number of successful
                  tasks to retain.
                format: int32
                type: integer
              suspend:
                default: false
                description: Suspend indicates whether the schedule is suspended.
                type: boolean
              targetRef:
                description: TargetRef identifies the target resource to invoke on
                  schedule.
                properties:
                  kind:
                    description: Kind is the kind of the target resource.
                    enum:
                    - Agent
                    - Workflow
                    - EvalRun
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    type: string
                required:
                - kind
                - name
                type: object
              taskTemplate:
                description: TaskTemplate defines the template for task execution.
                properties:
                  input:
                    description: Input is the input message to pass to the target.
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Parameters is a map of additional parameters for
                      the task.
                    type: object
                type: object
              timezone:
                default: UTC
                description: Timezone is the timezone for interpreting the cron schedule.
                type: string
            required:
            - schedule
            - targetRef
            type: object
          status:
            description: ScheduleStatus defines the observed state of a Schedule.
            properties:
              activeTaskRefs:
                description: ActiveTaskRefs is a list of references to currently active
                  tasks.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the schedule's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastScheduleTime:
                description: LastScheduleTime is the last time a task was successfully
                  scheduled.
                format: date-time
                type: string
              missedScheduleCount:
                description: MissedScheduleCount is the number of times the schedule
                  was missed.
                format: int64
                type: integer
              nextScheduleTime:
                description: NextScheduleTime is the next time a task is scheduled
                  to run.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
