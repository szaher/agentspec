---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workflows.agentspec.io
spec:
  group: agentspec.io
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.name
      name: NAME
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - description: Number of steps in the workflow
      jsonPath: .spec.steps[*]
      name: STEPS
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the workflows API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow.
            properties:
              failFast:
                default: true
                description: FailFast controls whether the workflow stops on the first
                  step failure.
                type: boolean
              finally:
                description: Finally lists steps that always run after the main steps
                  complete, regardless of failure.
                items:
                  description: WorkflowStep defines a single step in a workflow.
                  properties:
                    agentRef:
                      description: AgentRef is the name of the AgentSpec resource
                        this step invokes.
                      type: string
                    dependsOn:
                      description: DependsOn lists step names that must complete before
                        this step runs.
                      items:
                        type: string
                      type: array
                    input:
                      description: Input is the input data to pass to the agent.
                      type: string
                    name:
                      description: Name is the unique identifier for this step.
                      type: string
                    timeout:
                      default: 5m
                      description: Timeout is the maximum duration for this step.
                      type: string
                  required:
                  - agentRef
                  - name
                  type: object
                type: array
              steps:
                description: Steps is the ordered list of workflow steps to execute.
                items:
                  description: WorkflowStep defines a single step in a workflow.
                  properties:
                    agentRef:
                      description: AgentRef is the name of the AgentSpec resource
                        this step invokes.
                      type: string
                    dependsOn:
                      description: DependsOn lists step names that must complete before
                        this step runs.
                      items:
                        type: string
                      type: array
                    input:
                      description: Input is the input data to pass to the agent.
                      type: string
                    name:
                      description: Name is the unique identifier for this step.
                      type: string
                    timeout:
                      default: 5m
                      description: Timeout is the maximum duration for this step.
                      type: string
                  required:
                  - agentRef
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - steps
            type: object
          status:
            description: WorkflowStatus defines the observed state of Workflow.
            properties:
              completionTime:
                description: CompletionTime is the time the workflow finished executing.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the workflow's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: Phase is the current phase of the workflow (Pending,
                  Running, Succeeded, Failed).
                type: string
              startTime:
                description: StartTime is the time the workflow started executing.
                format: date-time
                type: string
              stepStatuses:
                description: StepStatuses contains the status of each step in the
                  workflow.
                items:
                  description: StepStatus describes the observed state of a single
                    workflow step.
                  properties:
                    completionTime:
                      description: CompletionTime is the time the step finished executing.
                      format: date-time
                      type: string
                    name:
                      description: Name is the name of the step.
                      type: string
                    output:
                      description: Output is the output produced by the step.
                      type: string
                    phase:
                      description: Phase is the current phase of the step (Pending,
                        Running, Succeeded, Failed).
                      type: string
                    startTime:
                      description: StartTime is the time the step started executing.
                      format: date-time
                      type: string
                    taskRef:
                      description: TaskRef is a reference to the underlying task resource
                        created for this step.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
